import { ReactElement } from 'react';
import { Notes } from 'features';

const App = (): ReactElement => {
  return (
    <div>
      <p>SOME APP!</p>
      <Notes />
    </div>
  );
};

export default App;
